# Work status - single place for done, in progress, critique

**Purpose:** One file that Manus, Codex, and Cursor read and update. No pasting of responses; sync via git.  
**Workflow:** See AI_TEAM_WORKFLOW.md in this folder.

---

## How to use this file

- **Before you start work:** Read PLATFORM_SOURCE_OF_TRUTH.md and this file (full).
- **When you complete something:** Add a dated entry under Done (who, what, commit/PR if any).
- **When you start something:** Add or update In progress (who, what).
- **When something is blocked or needs a decision:** Add to Blocked / needs decision.
- **When you review another's work:** Add to Critique / review (date, who you are commenting on, what you checked, any issues or suggestions). The next agent or Job can then act on it.

---

## Current priorities (from CEO)

1. Analytics instrumentation (ResusGPS and others to analyticsEvents; admin reports show real activity)
2. Staging (develop to staging, main to production)
3. Security baseline (password, session, audit logging)
4. ResusGPS v4 (undo, medication dedup, multi-diagnosis, structured age, countdown timers, dose rationale)

---

## Done

| Date | Who | What | Commit/PR |
|------|-----|------|----------|
| 2026-04-17 | Cursor | **Faster first paint / route transitions:** Hydrated `auth.me` from `localStorage` (stale-while-revalidate) in `_core/hooks/useAuth.ts` so returning users skip the auth loading shell; `RootEntry` returns nothing once session is known (no duplicate “routing” card). Lazy-loaded `Home` and `Payment`; Vite `manualChunks` for react-dom, TanStack Query, tRPC, and Lucide. Main entry chunk ~127 kB (gz ~38 kB) vs previous monolith. Added `client/src/lib/auth-session-cache.ts`. | `3ce8278` |
| 2026-04-17 | Cursor | **STK sub-10s “outside the box” (infra + warm path):** Added `GET /api/health` for uptime pings (keeps hosts like Render warm), scheduled Daraja OAuth refresh every 12 minutes while the scheduler runs, and earlier `warmDarajaAuth` on `/payment` when STK is offered. Documented ping + always-on in `.env.example`. | `18e8152` |
| 2026-04-17 | Cursor | **M-Pesa STK perceived latency:** Added `mpesa.warmDarajaAuth` (prefetch OAuth token when the M-Pesa form mounts), HTTPS keep-alive + 45s timeout for Daraja calls in `server/mpesa-real.ts`, parallelized OAuth cache fill before STK in `initiatePayment`/`retryPayment`/`enrollWithPayment`, deferred `trackPaymentInitiation` so the client gets success JSON sooner, and clearer copy in `MpesaPaymentForm` for cold start vs Safaricom delay. | `9a8e404` |
| 2026-04-17 | Cursor | **PWA console hygiene + manifest icons:** Added `mobile-web-app-capable` meta in `client/index.html`; pointed `manifest.json`, `service-worker.js`, and `pwa.ts` at existing `/favicon.png` (removed broken `/icon-192.png` and missing screenshot URLs) so Chrome stops reporting invalid manifest icons. | `c90ccb7` |
| 2026-04-17 | Cursor | **Faster M-Pesa STK initiation (client):** Split tRPC transport in `client/src/main.tsx` so payment-critical procedures (`mpesa.*`, enrollment/payments STK paths) use `httpLink` without batching, while other calls keep batched `httpBatchLink` with `maxURLLength: 2048`. Reduces queueing behind analytics `events.trackEvent` and should shorten time-to-STK-prompt when the network is busy. | `c90ccb7` |
| 2026-04-17 | Cursor | **tRPC batch HTTP 414 on checkout:** Set `maxURLLength: 2048` on `httpBatchLink` in `client/src/main.tsx` so large mutation batches (many `events.trackEvent` plus `mpesa.initiatePayment`) split before the comma-separated procedure path exceeds reverse-proxy URL limits (HTTP 414). | `954954e` |
| 2026-04-17 | Cursor | **Startup course seeding:** Fixed `initializeDatabase` to use `await getDb()` instead of the synchronous `db` proxy so seeding runs after the pool exists (removes recurring “Database client not initialized” noise in Render logs). | `954954e` |
| 2026-04-17 | Cursor | **M-Pesa STK client resilience:** Hardened the tRPC HTTP transport in `client/src/main.tsx` so empty API responses (common with gateway timeouts or deploys during slow Daraja calls) surface a clear operator-facing error instead of the browser’s cryptic `Unexpected end of JSON input`, and trigger the same www/apex API base fallback as network failures. | `d39b87f` |
| 2026-04-16 | Cursor | **Final audit synthesis + best-way-forward plan:** Consolidated Cursor's completed UX audits and Manus' reports into one truth-filtered execution plan in `docs/FINAL_BEST_WAY_FORWARD_PLAN.md`, separating confirmed current issues, overstated/false claims, and strategic institutional-grade backlog. This is now the recommended basis for the next execution cycle. | `d8a7542` |
| 2026-04-16 | Cursor | **Best-way-forward Phase 1 lock-in (ResusGPS scope + auth redirects + provider home):** Tightened `/resus` access to provider-only via `RoleGate` while preserving multi-role UI switching, wired all auth-entry routes through a shared safe redirect helper (`sanitizeNextPath`/`buildLoginUrl`) with tests in `client/src/lib/authRedirect.test.ts`, and confirmed the new provider home summary model (state badges, single next action, and explicit “why this is your next step” explanation). | `3a4a04f` |
| 2026-04-16 | Cursor | **Best-way-forward Phase 2/3 execution slice (institutional trust + parent follow-up + credentialed smoke):** Rebuilt institutional-facing copy to remove unsupported outcome claims and fake testimonial/case-study style content, strengthened `About`/`Help` trust and support packaging, made submission confirmation follow-up role-specific for parent vs provider, added parent “After you submit” clarity block, and fixed stale cross-account role carry-over on login/register by resetting stored `userRole` at auth entry. Verification: `pnpm run check`, `pnpm build`, and full credentialed local smoke pass (`http://localhost:3000`) across provider, parent, institution, instructor, help, and about. |  |
| 2026-04-16 | Cursor | **Best-way-forward Phase 4 polish + closure:** Added first-time onboarding guidance on `/start` (quick three-step orientation), finalized BWF execution tracking to `Done` in `docs/BEST_WAY_FORWARD_EXECUTION_PLAN.md`, and closed the verification/work-log/commit/push loop for this execution tranche. |  |
| 2026-04-20 | Manus | **Fellowship UX/UI Continuation**: Implemented server-backed publishability contract and generic course player. Added `isPublished` to `microCourses` table, updated catalog sync, and enhanced `FellowshipDashboard` to drive "Coming soon" gating from DB truth. Created `CourseGenericMicro` to handle unmapped courses and updated routing in `App.tsx` and `shared/provider-course-routes.ts`. |  |
| 2026-04-16 | Cursor | **Post-deploy production smoke evidence (latest `main`):** Verified on `https://www.paedsresus.com` that `/start` shows the first-time 3-step guidance, `/institutional` reflects trust-scoped copy and no testimonial/case-study claims, `/login` and `/register` are accessible, fresh provider signup/login lands on provider home (no stale role carry-over), provider/parent/institution/instructor lane routes load, and `/help` + `/about` render support/trust content. |  |
| 2026-04-16 | Cursor | **UX re-audit execution closure:** Implemented and verified the confirmed post-audit fixes by consolidating provider-home next-action logic into `dashboards.getSummary` / `getNextAction`, refactoring `ProviderDashboard` to a single summary query with explicit loading/error states, making protected-route loading copy route-aware, making `ResusGated` copy role-aware for parent/institution users, and locking auth-entry URLs to local email/password with `next` preservation. Verification: `pnpm run check`, `pnpm build`, `vitest` for `server/lib/provider-home-summary.test.ts`, and live browser smoke for `/start`, `/login`, and `/resus`. | `536f31f` |
| 2026-04-13 | Cursor | **Post-TypeScript-fix UX/UI re-audit:** Re-reviewed login, provider home, ResusGPS gating, role isolation, and protected-route behavior in code and live browser testing. Confirmed login messaging and role routing improvements, found no P0 blocker in tested public/protected-entry flows, and documented the remaining provider-home summary/performance gap in `docs/UX_UI_REAUDIT_POST_TYPESCRIPT_FIXES.md`. | `6fa18b9` |
| 2026-04-15 | Cursor | **Platform TypeScript cleanup:** Fixed the remaining platform typecheck blockers across provider enrollment, payment polling, admin reports, M-Pesa reconciliation, enrollment DB helpers, audit/streak utilities, course initialization, fellowship session inserts, and `ResusGPS`, bringing `pnpm run check` back to green with no lints on touched files. | `3a15c78` |
| 2026-04-14 | Cursor | **P0-7 operational closure prep slice:** Added a ready-to-fill sign-off minute template to `UX_DELIVERY_REFRAME_PHASE0_P0-7_SIGNOFF.md` for immediate Go/No-Go recording in `WORK_STATUS.md` after the sync call, including a companion No-Go blocker entry template. | `d1f7c4d` |
| 2026-04-14 | Cursor | **Final execution evidence pack slice:** Ran `db:test-connection`, `verify:analytics`, and `verify:ux-hardening` as a final closure evidence pass; all passed. Added `UX_REFRAME_FINAL_EXECUTION_EVIDENCE.md` documenting outcomes and linked Phase 0 tracker P0-7 note to this evidence pack. | `a27a3f1` |
| 2026-04-14 | Cursor | **Phase 0 tracking closure slice - Cursor-owned tasks marked done:** Updated `UX_DELIVERY_REFRAME_PHASE0_EXECUTION_PLAN.md` to mark P0-2 (state machine), P0-4 (dependency DAG), and P0-6 (rollback protocol) as `Done` with implementation-backed notes, and updated `WORK_STATUS` In progress text to reflect final closure/handoff while awaiting cross-team P0-7 sign-off scheduling. | `e2a9bc5` |
| 2026-04-14 | Cursor | **Residual operational-doc cleanup slice - institutional route references:** Normalized lingering `/institutional-portal` mentions across operational/audit docs (`UNLINKED_PAGES_ANALYSIS.md`, `EXECUTION_PLAN_CURSOR_AND_MANUS.md`, `INSTITUTIONAL_PLATFORM_AUDIT.md`, `MANUS_HIDDEN_OPPORTUNITIES_REPORT.md`, `ORPHANED_PAGES_AUDIT_E1.md`) to canonical `/hospital-admin-dashboard` semantics with legacy redirect notes where historically relevant. | `d6ae7eb` |
| 2026-04-14 | Cursor | **Governance docs closure slice - institutional lane consistency:** Updated `CEO_Platform_Update_And_Reply_To_AI_Team.md`, `PROMPT_FOR_MANUS_HIDDEN_OPPORTUNITIES.md`, and `UX_DELIVERY_REFRAME_PHASE0_P0-3_ACCESS_MATRIX.md` to reflect institutional canonical route `/hospital-admin-dashboard` with `/institutional-portal` treated as legacy redirect compatibility. This closes remaining high-signal route-policy drift across execution/governance docs. | `3d6e92b` |
| 2026-04-14 | Cursor | **Week 4 route-consistency closure slice - institutional nav canonicalization:** Removed remaining live UI references to `/institutional-portal` by updating institutional header/nav and navigation constants to use `/hospital-admin-dashboard` as canonical destination, while retaining `/institutional-portal` only as documented legacy redirect route. Verified with `verify:ux-hardening` and lints. | `1abf483` |
| 2026-04-14 | Cursor | **Canonical docs alignment slice - institutional default route:** Updated `PLATFORM_SOURCE_OF_TRUTH.md` and `ENGINEERING_ACCEPTANCE_CHECKLIST.md` to reflect the implemented institutional default landing route (`/hospital-admin-dashboard`) while explicitly documenting legacy `/institutional-portal` as redirect-only. This removes policy/implementation drift for auth-entry and acceptance checks. | `41fcb1e` |
| 2026-04-14 | Cursor | **Week 4 lane-integrity closure slice - ResusGPS/help/admin fallbacks:** Made shared fallback navigation role-safe by routing `ResusGated` “Back to hub” to role-specific homes (provider/parent/institution), switching `Help` dashboard CTA to root entry (`/`) for auth-aware lane routing, and updating admin page non-admin fallbacks (`AdminHub`, `AdminReports`, `AdminMpesaReconciliation`) to use role-aware root instead of hardcoded provider home. Verified with `verify:ux-hardening` and lints. | `67a9767` |
| 2026-04-14 | Cursor | **Week 4 lane-integrity closure slice - role-aware root/admin fallbacks:** Updated `App.tsx` to centralize role-home routing (`provider -> /home`, `parent -> /parent-safe-truth`, `institution -> /hospital-admin-dashboard`) and applied it across `RootEntry`, `RoleGate` restricted CTA, and `AdminGate` non-admin fallback. This removes remaining authenticated wrong-lane redirects to provider home for parent/institution users. Verified with `verify:ux-hardening` and lints. | `eb32191` |
| 2026-04-14 | Cursor | **Week 4 wrong-lane link cleanup slice - shared support/resources:** Removed remaining provider-only dashboard links from shared and institutional contexts by updating `Help.tsx` dashboard CTA to `/home`, institutional “View Resources” CTA to `/help`, `ProtectedPageWrapper` fallback resource CTA to `/help`, and `DashboardSidebar` FAQ link to `/help`. This reduces accidental role detours for parent/institution users from support surfaces. Verified with `verify:ux-hardening` and lints. | `4194a1e` |
| 2026-04-14 | Cursor | **Week 4 lane-integrity navigation slice - role-safe mobile/home routing:** Fixed wrong-lane role navigation defaults by updating mobile `BottomNav` parent lane away from provider-only `/learner-dashboard`, setting institution home lane directly to `/hospital-admin-dashboard`, and reducing institutional redirect loops in role fallback/routing (`Home.tsx`, `App.tsx`, `Header.tsx`). This removes avoidable cross-role detours and improves role-lane continuity on mobile and role switches. Verified with `verify:ux-hardening` and lints. | `e78a57c` |
| 2026-04-14 | Cursor | **Week 4 auth-entry continuity slice - return-path aware login/register:** Added `next` path preservation across auth boundaries by updating `RoleGate` and `AdminGate` login redirects (`/login?next=...`) and teaching `Login.tsx`/`Register.tsx` to honor safe in-app `next` destinations. Also auto-redirect authenticated users away from login/register to their target path, reducing auth-loop friction and making protected-route recovery deterministic. Updated `Home.tsx` unauth redirect to preserve `/home` return path. Verified with full `verify:ux-hardening` gate and lints. | `a4129e6` |
| 2026-04-14 | Cursor | **Week 4 release-hardening slice - one-command UX verification:** Added `verify:ux-hardening` script in `package.json` to run the core role/payment continuity regression suite (`role-boundary-provider-api`, `enrollment-payment-flows`, `EnrollmentModal.integration`, `providerCourseRoutes`) plus production build in one command. This creates a repeatable pre-deploy gate for the UX reframe execution stream. Verified by running the new script successfully. | `3a0681e` |
| 2026-04-14 | Cursor | **Week 4 execution slice - packaged root entry flow:** Routed root entry (`/` and catch-all) through auth-aware `RootEntry` in `App.tsx` so anonymous users are directed to `/start` and authenticated users to `/home`, reducing first-visit login dead-ends and aligning entry packaging with role-based journeys. Updated `Start.tsx` ResusGPS copy to match current signed-in access behavior. Verified with build and lints. | `3da5f8f` |
| 2026-04-14 | Cursor | **Week 2/3 hardening slice - automated role-leak regression tests:** Added `server/routers/role-boundary-provider-api.test.ts` to lock provider-only API boundary behavior across enrollment, courses, and instructor routers by asserting `FORBIDDEN` for parent/institution contexts. This makes UI/route/API role isolation protections enforceable in CI and reduces regression risk as routing and payment flows evolve. Verified with targeted Vitest suites and full build. | `6fbd2c2` |
| 2026-04-14 | Cursor | **Week 3 execution slice - payment continuity hardening:** Removed payment dead-end states in `Payment.tsx` by adding deterministic fallback actions: explicit CTA to `Enroll` when locked enrollment is missing, explicit CTA to `Fellowship` when a fellowship enrollment hits provider payment page, and a post-bank-transfer continuation CTA back into provider learning destination mapping. This ensures every payment exception/success branch still has a guided next step. Verified via build, lints, and enrollment/provider route regression tests. | `c0e0605` |
| 2026-04-14 | Cursor | **Week 2 execution slice - API-level role isolation for courses router:** Added provider/admin authorization guard in `server/routers/courses.ts` and enforced it for provider enrollment/payment procedures (`getMyAhaEnrollments`, `getEnrollments`, `getUserEnrollments`, `enroll`, `complete`, `initiateMpesaPayment`). This closes cross-role API execution paths for parent/institution users on provider learning/payment operations and aligns API behavior with route-level guards. Verified via build, lints, and enrollment payment regression suite. | `5889137` |
| 2026-04-14 | Cursor | **Week 2 execution slice - role-route closure and nav leak cleanup:** Expanded `RoleGate` coverage in `App.tsx` for provider-only clinical surfaces (`patients`, `protocols`, `performance-dashboard`, `cpr-monitoring`, referral/analytics and learning diagnostics routes) and institution-only analytics route, plus parent/provider-only `personal-impact` access policy. Updated legacy `/resources` redirect to `/help` to avoid cross-role learner-dashboard detours. Hardened account dropdown in `Header.tsx` so parent/institution users no longer see provider profile navigation. Verified via build, lints, and provider route regression tests. | `86d3b89` |
| 2026-04-14 | Cursor | **Week 2 execution slice - next-action state machine + role boundary hardening:** Upgraded `ProviderDashboard` from static menu to deterministic provider next-best-action model (resume unpaid AHA payment -> start paid micro-course -> continue in-progress micro-course -> renew ResusGPS -> open ResusGPS), with conversion analytics on each primary CTA. Removed parent dead-end course nav entry in header and replaced with parent-safe links. Added API-layer role protection for provider-only enrollment/payment surfaces in `enrollment` router (`create`, `recordPayment`, `getPaymentsByEnrollmentId`, `enrollWithPayment`, `submitManualMpesaReference`, `validatePromo`) and provider-only instructor surfaces in `instructor` router. Verified with `pnpm build`, `EnrollmentModal` integration tests, provider route tests, and enrollment payment flow tests. | `ca63e12` |
| 2026-04-14 | Cursor | **Week 1 execution started - auth/performance/role guards hardening:** Removed forced full-page reload on password login (`Login.tsx`) to reduce sign-in latency. Added route-level role enforcement in `App.tsx` (`RoleGate` and `AdminGate`) for provider/institution/admin-sensitive surfaces to block cross-role route access drift. Reduced fellowship dashboard startup load by delaying heavy courses/enrollments queries until the courses tab is opened and setting sane query cache windows. Fixed `ResusGated` infinite-loading failure mode by handling query errors explicitly and adding retry/fallback actions; added slow-load retry affordance. Moved localStorage auth cache write out of `useMemo` into `useEffect` in `_core/useAuth` to avoid render-side effects. Verified build + key enrollment/provider regression suites. | `c07384b` |
| 2026-04-14 | Cursor | **Phase 0 gap-closure refinements (Manus deep-dive):** Closed execution-detail gaps by specifying profiling baseline controls (realistic DB volume, cold-cache baseline, single-user baseline), state-machine edge-case rules (multiple unpaid, expired ResusGPS, Care Signal priority), role-matrix cross-role policy + baseline inventory counts, DAG owner map + sub-task granularity + contingency impact estimate, expanded role/perf test strategy with instrumentation and pass/fail gates, and rollback runbook thresholds (30-minute canary window, login >5% trigger). Confirmed P0-7 as a 60-minute sync call with explicit Go/No-Go criteria. | `2ed2237` |
| 2026-04-14 | Cursor | **Phase 0 execution hardening from Manus/Job review:** Upgraded Phase 0 from static planning to active execution by adding detailed artifacts for P0-1/P0-3/P0-4/P0-5/P0-6 (profiling method, access matrix scaffold, dependency DAG, test strategy, rollback runbook), locking timeline milestones (2026-04-14 start, 2026-04-17 09:00 UTC sign-off), and marking P0 owner tasks as in-progress in the execution tracker. | `ff9fb24` |
| 2026-04-14 | Cursor | **UX delivery reframe - Phase 0 kickoff artifacts:** Added `UX_DELIVERY_REFRAME_PHASE0_SPEC.md` (scope, SLO/FMC definitions, role-leak definition, provider state machine, deliverables, Week 1 entry criteria) and `UX_DELIVERY_REFRAME_PHASE0_EXECUTION_PLAN.md` (task table, owners, status tracking, phase sign-off gate) to operationalize the approved reframe direction before implementation. | `fec8b5a` |
| 2026-04-13 | Cursor | **Manual Lipa na M-Pesa fallback (micro-course checkout):** Added explicit manual Paybill fallback in `EnrollmentModal` payment step (Paybill/account instructions + payer phone + M-Pesa receipt submission), plus backend capture endpoint `enrollment.submitManualMpesaReference` to persist receipt codes on `microCourseEnrollments` for reconciliation. Extended `enrollWithPayment` with `paymentPath` to support manual-lipa enrollment creation without STK initiation and exposed client-safe Paybill/account metadata via `mpesa.getClientPaymentCapabilities`. Verified build + enrollment integration regressions. | `6d51097` |
| 2026-04-13 | Cursor | **Production auth connectivity hardening:** Added resilient tRPC transport fallback in `client/src/main.tsx` so browser `Failed to fetch` on primary API base retries alternate canonical `/api/trpc` origins (`www` <-> apex + same-origin), reducing auth/payment outages from domain/API endpoint misalignment. Improved login/register network-failure messaging for clearer operator/user diagnosis. Verified build + core frontend regressions. | `e1b44d2` |
| 2026-04-13 | Cursor | **Sample micro-course end-to-end (Intubation Essentials):** Added a real fellowship sample micro-course (`intubation-essentials`) to the catalog and dashboard flow, created idempotent course/module/quiz seeding (`ensure-intubation-sample-course-catalog`), added dedicated learning route/page (`/course/intubation-essentials`), mapped provider navigation, and linked micro-course enrollment/payment to a fellowship learning enrollment so module completion can trigger certificate eligibility through the existing certificate pipeline. Updated micro-payment reconciliation to sync linked enrollment payment completion for this sample flow. Verified with build + enrollment/payment integration suites. | `c0dfe65` |
| 2026-04-13 | Cursor | **AHA payment resilience + frontend load hotfix:** Added network-failure recovery in provider checkout (`MpesaPaymentForm` + `Payment`) so M-Pesa `Failed to fetch` errors show actionable guidance and auto-fallback to bank transfer path. Improved load performance by introducing route-level lazy loading in `App.tsx` and making service worker opt-in (`VITE_ENABLE_SW=true`) while auto-unregistering/clearing legacy SW caches by default to prevent stale cache/network overhead. Build and enrollment/payment regression suites pass. | `b48edd3` |
| 2026-04-13 | Cursor | **Signin incident fix (users schema drift tolerance):** Updated `server/db.ts` user lookups (`getUserByEmail`, `getUserByOpenId`, `getUserById`) to gracefully fall back to a legacy-compatible column projection when optional `users` columns are missing, preventing auth crashes from `ER_BAD_FIELD_ERROR`. Expanded `server/fix-users-columns.mjs` and `drizzle/fix-users-columns-manual.sql` to include missing newer `users` columns (institutional role, instructor fields, `resusGpsAccessExpiresAt`) for permanent schema parity repair. | `b48edd3` |
| 2026-04-13 | Cursor | **Provider homepage simplification (two-button model):** Reworked `ProviderDashboard` into a single low-friction entry card with exactly two primary actions — `Paeds Resus Fellowship` and `AHA Courses` — so detail and secondary decisions happen only after entering the selected flow. | `a011c25` |
| 2026-04-13 | Cursor | **Provider microcopy consistency pass:** Aligned CTA verbs across `ProviderDashboard` and `AHACourses` to a tighter action set (`Pay now`, `Start ... enrollment`, `Start course`, `Open ...`) and simplified ResusGPS next-step wording to reduce ambiguity in the provider decision flow. | `363464f` |
| 2026-04-13 | Cursor | **Provider UX refinement (AHA label correction + homepage declutter):** Forced canonical AHA card copy labels by program type (BLS/ACLS/PALS) in `AHACourses` so `pals` no longer displays “The systematic approach to a seriously ill child”. Simplified `ProviderDashboard` decision surface by replacing 3 CTA-heavy pillar cards with one read-only status snapshot and one primary CTA for AHA, reducing decision overload. | `7b8a84b` |
| 2026-04-13 | Cursor | **Launch hardening (db compatibility test guard):** Added `server/db-compat-exports.test.ts` to assert `db.getUserById` and `db.createAuditLog` exports stay available for auth router compatibility, preventing silent reintroduction of undefined-import build warnings. | `a3b2b0d` |
| 2026-04-13 | Cursor | **Launch hardening (build warning cleanup):** Added missing `db` exports `getUserById` and backward-compatible `createAuditLog` in `server/db.ts` to match existing `server/routers.ts` callsites and remove esbuild `import-is-undefined` warnings for auth routes. | `74a3fa2` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.14 route helper test coverage):** Added targeted Vitest coverage for `client/src/lib/providerCourseRoutes.ts` to lock provider continuation mappings (BLS/ACLS/PALS/Paediatric Septic Shock/Instructor), fallback behavior, and AHA continue CTA config, reducing regression risk after route centralization. | `809fe67` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.13 route consistency hardening):** Added shared provider course route helper (`client/src/lib/providerCourseRoutes.ts`) and replaced duplicated deep-link logic in `Payment`, `AHACourses`, `EnrollmentModal`, `LearnerDashboard`, and `FellowshipDashboard` so post-payment and “continue learning” destinations stay consistent across BLS/ACLS/PALS/Instructor/Fellowship micro-course contexts. | `b0f4f67` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.12 universal post-payment deep-linking):** Extended `Payment` completion redirect logic so paid provider flows consistently deep-link into the correct learning destination across BLS, ACLS, PALS, and Instructor (with enrollment context where available), removing remaining post-payment dashboard detours. | `9aa7a33` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.11 payment track-aware copy):** Updated `Payment` headings/subtitles/course-selection labels to be track-aware (AHA vs Paeds Resus instructor) so provider checkout language always matches the selected enrollment path while preserving locked-context behavior. | `0ce4816` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.10 provider course taxonomy split):** Added explicit provider-track metadata in pricing (`aha_certification` vs `paeds_resus`) and restructured `Enroll` into clear sections (AHA certifications, Paeds Resus pathways, Fellowship handoff). Updated `Payment` to use track metadata for course selection instead of hardcoded IDs. | `526e0c0` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.9 ACLS direct learning route):** Added dedicated ACLS learning route (`/course/acls`) and wired ACLS paid continuation from both `AHACourses` and `Payment` to this route, removing the final BLS/PALS vs ACLS path asymmetry. | `8739206` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.8 AHA ACLS continuation clarity):** Fixed paid AHA CTA mismatch in `AHACourses` by making continuation labels destination-aware. BLS/PALS keep `Start course`, while ACLS now shows `Open learner dashboard` to match the actual route and reduce post-payment confusion. | `f20b1fb` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.7 provider checkout speed-up):** Streamlined `Enroll` checkout by removing redundant contact-form fields for authenticated providers and keeping a single confirmation step before payment. This reduces friction between course selection and M-Pesa initiation while preserving terms consent and enrollment integrity. | `d3df5e2` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.6 provider navigation declutter):** Reduced provider header navigation to core journey links (`Dashboard`, `Fellowship`, `AHA`, `ResusGPS`, `Care Signal`, `Instructor`) to cut top-nav overload. Redirected legacy `/courses` route to `/fellowship` so provider course traffic follows the single fellowship entry path. | `1dc354f` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.5 provider microcopy normalization):** Standardized provider action language across core journey surfaces (`ProviderDashboard`, `AHACourses`, `Payment`, `FellowshipDashboard`, `Enroll`) to reduce verb drift and ambiguity (`Pay now`, `Open Fellowship`, clearer enrollment-locked payment wording, one-path fellowship phrasing). | `51b4490` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.4 fellowship + home UX consistency):** Polished `FellowshipDashboard` and `Home` provider language to align with the single fellowship model and cleaner action wording. Fixed unsafe hook usage pattern in `FellowshipDashboard` (`trpc.useUtils` now top-level), replaced render-time redirect with effect-based redirect, and wired actionable continue buttons for enrolled courses in fellowship list. | `2df5be4` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.3 provider Enroll + LearnerDashboard simplification):** Simplified provider conversion copy and CTA hierarchy in `Enroll` and `LearnerDashboard`. `Enroll` now uses cleaner enrollment-to-payment language (removed noisy popularity/trust blocks, stronger “Continue to payment” flow). Provider `LearnerDashboard` cards were restructured to clearer hubs (`Fellowship`, `AHA`, `Care Signal`, `Instructor`) with fewer competing actions and less jargon. | `effc0e8` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.2 provider copy + CTA declutter):** Reduced provider decision noise and improved payment clarity. Removed duplicate AHA primary-action card on `ProviderDashboard`, tightened AHA page wording/CTA verbs (`Pay now`, `Start course`), and simplified `Payment` messaging around speed and expectations (faster M-Pesa guidance, clearer method descriptions, replaced bulky bottom feature cards with one focused guidance alert). | `9508cc4` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 2.1 provider journey cleanup):** Enforced strict provider split between Fellowship and AHA. Removed `pals_septic` from AHA pricing/payment path, locked payment context whenever `enrollmentId` is present, simplified provider header nav (`Fellowship` + `AHA`), decluttered `ProviderDashboard` by replacing crowded AHA card grid with a compact AHA hub block + single primary AHA action, improved AHA empty-state copy, and redirected legacy `/elite-fellowship` route to `/fellowship`. | `3d02714` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.30 production crash hotfix - React #310):** Fixed provider dashboard hook-order violation causing minified React error #310 in production. Moved `primaryAhaAction` `useMemo` above the early loading return in `ProviderDashboard` so hook order is stable across renders. Verified production build passes. | `4607cbb` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.29 deploy hotfix for AdminReports JSX text):** Fixed Render/Vite build failure by escaping JSX inline text containing `->` and `>=` in `AdminReports` launch-ops copy (`-&gt;`, `&gt;=`). Verified full production build passes locally. | `cbda155` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.28 fresh canary evidence guard):** Hardened lifecycle live-send safety by requiring a fresh (<=30 minutes) canary evidence snapshot in `AdminReports` before execution. Live dispatch now depends on dry-run match, `SEND`, `ACK ROLLBACK`, canary verification, and recent evidence freshness. Updated launch playbook with the 30-minute evidence freshness rule. | `3e40ba6` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.27 canary evidence capture):** Added canary evidence capture tools in `AdminReports` for launch operations (timestamped snapshot, copy latest evidence text, CSV export history). Snapshot records SMS/email canary metrics, fallback counts, canary gate state, and alert presence to support auditable first-live-cycle handoff. Updated launch playbook cadence + handoff template. | `d8d52c2` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.26 production canary verification gate):** Added canary verification safety gate in `AdminReports` for first live-cycle ramp. Live dispatch now requires external-channel canary readiness (SMS + email health/fallback checks) and explicit typed `CANARY PASS` acknowledgment in addition to existing dry-run, `SEND`, and `ACK ROLLBACK` guards. Updated launch playbook with canary gate policy. | `3d75524` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.25 rollback acknowledgment gate):** Added explicit rollback acknowledgment guard in `AdminReports` live dispatch safety checks. Live sends now require both `SEND` and `ACK ROLLBACK` confirmations (plus matching dry-run) before execution. Updated launch playbook to document this operator requirement. | `445fa15` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.24 launch-week ops playbook):** Added `docs/LIFECYCLE_CHANNEL_LAUNCH_PLAYBOOK.md` with first-week operating cadence, threshold actions, escalation SOP, canary resume criteria, rollback triggers, and handoff template. Added compact launch-week checklist card in `AdminReports` to keep operators aligned with the SOP during live runs. | `a8d0000` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.23 degraded-channel fallback rules):** Added automated degraded-channel fallback in lifecycle dispatcher: when recent external-channel delivery health drops below threshold, SMS/email sends are temporarily suppressed (in-app still sent), and fallback markers are recorded in telemetry (`channelReliability.fallbackPolicy`, `*_channel_degraded_fallback`). Extended lifecycle summary + `AdminReports` with `channelFallbackActivations` for operator tracking. Updated taxonomy docs. | `826ecd0` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.22 channel health alerts):** Added `channelHealthAlerts` to lifecycle dispatch summary using per-channel delivery-rate thresholds (warning/critical) from sent/failed telemetry. `AdminReports` now surfaces actionable operator alerts for low-delivery channels, with fallback “all clear” state when no alerts are present. Updated taxonomy docs. | `8348e1c` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.21 channel failure diagnostics):** Extended `notifications.getLifecycleDispatchSummary` with `channelFailureTop` (top failed-channel reasons per medium from `channelReliability.lastError` in timed nudge analytics) and added `AdminReports` diagnostics table for fastest operator triage of SMS/email/in-app delivery failures. Updated taxonomy docs. | `7c58234` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.20 channel reliability + trend):** Added lifecycle channel reliability instrumentation in `lifecycle_timed_nudge_sent` payload (`channelReliability` with retry policy, attempts, and lastError for SMS/email/in-app context). Implemented retry-aware SMS/email dispatch attempts in lifecycle executor and expanded `AdminReports` with 7d vs 30d channel delivery health trend (delivery rate computed from sent/failed outcomes). Updated taxonomy docs. | `a2bfa4e` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.19 channel outcomes reporting):** Extended `notifications.getLifecycleDispatchSummary` to aggregate timed lifecycle nudge channel outcomes from analytics payloads (`inApp`, `sms`, `email` sent/failed/skipped) and return `totalTimedNudges` + `channelTotals`. Updated `AdminReports` lifecycle dispatch panel to display this channel performance table and tracked-period count for operator visibility. Updated taxonomy docs. | `108eb8f` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.18 channelized lifecycle sends - email):** Extended lifecycle nudge dispatch to include **email** channel in the same idempotent executor. Dispatcher now uses user email + name, respects `emailNotifications`, sends templated lifecycle nudge email with deep-link CTA, and records per-channel outcome (`inApp`/`sms`/`email`) in `provider_conversion/lifecycle_timed_nudge_sent` payload. Added `providerLifecycleNudge` email template and updated taxonomy docs. | `b809c61` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.17 channelized lifecycle sends):** Extended lifecycle nudge dispatcher to send channelized nudges as **in-app + SMS** in one idempotent flow. `notifications.dispatchLifecycleNudges*` now loads recipient phone, respects user notification preferences, attempts SMS send via `server/sms.ts` (`messageType: lifecycle_nudge`), and records per-channel outcome (`inApp`/`sms`) on `provider_conversion/lifecycle_timed_nudge_sent` event payload for operator visibility. Updated `EVENT_TAXONOMY.md`. | `9bd410a` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.16 runbook automation polish):** Enhanced lifecycle batch dispatch UX in `AdminReports` with explicit recommended operator sequence banner and one-click "Copy run summary" action (mode, limits, processed/sent/skipped) for fast ops handoff/reporting. Keeps existing dry-run/live safeguards intact while reducing operator friction. Regression tests remain green (30/30 key suites). | `97bbb42` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.15 operator safeguards):** Added live-send safety gating to lifecycle batch dispatch UI in `AdminReports`: live execution now requires a matching dry-run (same limits, within 30 minutes) plus explicit typed confirmation (`SEND`). Added guard feedback text and automatic confirmation reset when limits change or live send completes, reducing accidental bulk sends while preserving fast operator flow. Regression tests remain green (30/30 key suites). | `a735a0e` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.14 run-level ops analytics):** Added server-side run telemetry for lifecycle batch dispatch (`admin_ops/lifecycle_batch_dispatch_run`) and new admin summary query `notifications.getLifecycleDispatchSummary` (rolling-window runs, live vs dry-run counts, processed users, sent/skipped totals, recent run rows). Wired `AdminReports` summary UI to show backend-truth run metrics and recent runs table, complementing local UI history/export. Updated `EVENT_TAXONOMY.md` with `admin_ops` coverage. Regression tests remain green (30/30 key suites). | `67540dc` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.13 ops reporting loop):** Extended `AdminReports` lifecycle dispatch panel with ops reporting utilities: CSV export for current run detail, CSV export for historical runs, and timestamped persistent run history (mode, limits, processed/sent/skipped) stored client-side for quick weekly review. Added in-panel history clear action. Regression tests remain green (30/30 key suites). | `fe5d4f3` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.12 admin operator UI):** Added lifecycle batch-dispatch control panel to `AdminReports` with dry-run preview and live execute actions for `notifications.dispatchLifecycleNudgesBatch`. UI includes configurable user/nudge limits, run-status feedback, and summarized + per-user results table (processed/sent/skipped) to support weekly ops cadence without terminal usage. Regression tests remain green (30/30 key suites). | `d47c139` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.11 ops batch dispatch):** Added admin-only batch executor `notifications.dispatchLifecycleNudgesBatch` with dry-run support and per-user limits, built on shared lifecycle dispatch helpers. Batch run targets users with active paid/free micro-course enrollments, applies existing idempotency keys (`enrollment + nudge type + cadence`), and returns aggregated send/skip metrics plus per-user reports for operations visibility. Regression tests remain green (30/30 key suites). | `a5ec9cc` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.10 lifecycle comms executor):** Added `notifications.dispatchLifecycleNudges` mutation to execute 24h/72h provider lifecycle nudges with idempotency (dedupe keyed by enrollment + nudge type + cadence against prior `provider_conversion/lifecycle_timed_nudge_sent` analytics rows). Dispatcher creates in-app course update notifications with deterministic action URLs and emits send analytics. `LearnerDashboard` now triggers dispatch when due nudges exist and logs dispatch telemetry; taxonomy changelog updated. Regression tests remain green (30/30 key suites). | `bc35f98` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.9 lifecycle automation scaffolding):** Added server-side lifecycle nudge computation endpoint `notifications.getLifecycleNudges` (24h/72h cadence) for provider micro-course states (`paid_not_started`, `started_not_completed`) using `microCourseEnrollments` timing anchors. Wired timed in-app reminder card on provider `LearnerDashboard` with deterministic resume/start CTA and conversion tracking event `lifecycle_timed_nudge_clicked`. Updated `EVENT_TAXONOMY.md` changelog for lifecycle conversion events. Regression tests remain green (30/30 key suites). | `b14511d` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.8 in-app lifecycle nudges):** Added provider `LearnerDashboard` recovery nudges for `paid_not_started` and `started_not_completed` states with one primary resume/start CTA routed to the best available course continuation path (course deep-link where supported, otherwise `/fellowship`). Added funnel tracking event `lifecycle_resume_nudge_clicked` with nudge type and destination metadata for lifecycle conversion analysis. Regression tests remain green (30/30 key suites). | `429ca74` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.7 completion-triggered upsell):** Added in-flow upsell at final module completion in `LearningPath`. After passing the last module quiz, learners now see an immediate next-course purchase recommendation (prioritized to not-yet-enrolled foundational micro-courses) with one-click deep-link to preselected enroll checkout (`/enroll?courseId=...`). Added provider conversion tracking event `completion_upsell_clicked` to measure course-completion -> second-purchase intent. Regression tests remain green (30/30 key suites). | `a9896bd` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.6 second-purchase trigger):** Added provider-facing "next recommended course" card on `LearnerDashboard` once a learner has at least one paid/completed micro-course. Recommendation prioritizes not-yet-enrolled foundational courses and routes directly to preselected enroll checkout (`/enroll?courseId=...`). Added provider conversion tracking event `second_purchase_recommendation_clicked` for second-purchase funnel measurement. Regression tests remain green (30/30 key suites). | `d1cec7e` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.5 activation assurance):** Fixed post-enrollment success dead-end in `EnrollmentModal` by turning the primary success CTA into deterministic course launch (`/course/bls`, `/course/seriously-ill-child`, `/course/paediatric-septic-shock`, `/course/instructor` with `enrollmentId` when applicable; fallback `/learner-dashboard`). Added analytics event for success CTA click (`enrollment_success_primary_cta_clicked`) and removed misleading “Download Receipt” action from this flow. Regression tests remain green (30/30 key suites). | `2d62f11` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.4 payment confidence + recovery):** Hardened M-Pesa reconciliation UX to avoid dead-end drop-off: timeout/failure no longer force-exits to payment screen; users now get explicit in-context recovery actions (check again vs retry payment), clearer pending countdown copy, and stronger support guidance. In `Payment`, wired bank-transfer fallback action end-to-end (copy bank details + submit bank-transfer confirmation against the locked enrollment with optional reference). Regression tests remain green (30/30 key suites). | `1e5f1e5` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.3 primary-action model):** Added one dominant AHA next-step card on provider surfaces (`ProviderDashboard`, `AHACourses`) with prioritized action selection (pending payment first, otherwise next enrollment). Demoted supporting pillar CTAs to secondary styling so the primary conversion action is visually unambiguous while preserving access. Regression tests remain green (30/30 key suites). | `f0ac4a8` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.2 continuity):** hardened provider CTA continuity across AHA surfaces: `ProviderDashboard` and `AHACourses` now route unpaid enrollments to enrollment-aware payment links (`/payment?enrollmentId=...&courseId=...`) and paid learners to best-available deep links (`/course/bls` or `/course/seriously-ill-child` where applicable). Added `/enroll?courseId=...` preselect + direct checkout start in `Enroll` to keep CTA flows linear. Regression tests remain green (30/30 key suites). | `46b5bbb` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1.1 payment instrumentation):** Added provider conversion analytics on [`Payment`](../client/src/pages/Payment.tsx) for course selection, payment method selection, and post-M-Pesa completion redirect events (`payment_course_selected`, `payment_method_selected`, `payment_completed_redirect`) to close the funnel measurement gap between enroll modal and course launch. Regression tests green (30/30 across key enrollment suites). | `2bcf799` |
| 2026-04-13 | Cursor | **Conversion roadmap implementation (Phase 1 slice):** Added provider funnel analytics hook [`useProviderConversionAnalytics`](../client/src/hooks/useProviderConversionAnalytics.ts), instrumented provider role selection (`Home` + `auth.updateUserType` server event), provider dashboard CTA events, micro-course enrollment modal lifecycle events (started/initiated/failed/completed), and learning path progression events (course/module started, module completed). Updated [EVENT_TAXONOMY.md](./EVENT_TAXONOMY.md) with `provider_conversion`. Verified targeted tests: enrollment payment flows 13/13 and EnrollmentModal integration 17/17. | `bf550c4` |
| 2026-04-13 | Cursor | **Conversion roadmap integrated into PSoT without dilution:** Added PSoT §18 "Conversion and recurring revenue execution model (provider)" as governance + KPI-layer only (scope, precedence, non-negotiable constraints), and created [CONVERSION_90_DAY_EXECUTION_PLAN.md](./CONVERSION_90_DAY_EXECUTION_PLAN.md) for executable 90-day phases, experiments, owners, and cadence. | `160eebf` |
| 2026-04-13 | Cursor | **Enrollment retry + stuck M-Pesa polling fix (micro-courses):** `isUserEnrolled` now blocks only paid/free enrollments (not pending), `enrollWithPayment` reuses pending M-Pesa enrollment and returns existing `checkoutRequestId`, and stores `checkoutRequestId` on enrollment when STK starts. Added `mpesa.checkPaymentStatus` + `mpesa.reconcilePayment` procedures used by `MpesaReconciliationStatus`; fixed modal amount/enrollment typing and polling loop so retries no longer dead-end on “Already enrolled” while payment is pending. | `d88ebdc` |
| 2026-04-11 | Cursor | **Backend enrollment payment tests (CI) + catalog on Render:** `ensureMicroCoursesCatalog` used `db.query.microCourses`, but app `drizzle(pool)` has no `{ schema }`, so `db.query.*` was undefined whenever `DATABASE_URL` was set — enroll/tests failed with `findFirst` / wrong errors. Switched catalog ensure to `select`/`update`/`insert`. `createEnrollment` now returns normalized `{ insertId }` from mysql2/Drizzle insert shape. `enrollment-payment-flows.test.ts` partially mocks `ensureMicroCoursesCatalog` so unit tests never require DB. | `1840706` |
| 2026-04-13 | Cursor | **Provider product: Fellowship vs AHA + real micro-courses:** Single catalog [`server/lib/micro-course-catalog.ts`](../server/lib/micro-course-catalog.ts) (200 KES cents) + `ensureMicroCoursesCatalog` on list/pay; [`enrollWithPayment`](../server/routers/enrollment.ts) calls ensure before lookup; [`courses.listAhaHubPrograms`](../server/routers/courses.ts) + `getMyAhaEnrollments` for BLS/ACLS/PALS; [`ResusGated`](../client/src/pages/ResusGated.tsx) + [`getResusGpsAccessForClient`](../server/lib/resusgps-access.ts) (30-day window; optional `RESUSGPS_STRICT_FELLOWSHIP_WINDOW`); provider [`Home`](../client/src/pages/Home.tsx) → [`ProviderDashboard`](../client/src/pages/ProviderDashboard.tsx); `/resus` restored in App. Migration `0033` `resusGpsAccessExpiresAt`. | `3c7a565` |
| 2026-04-13 | Cursor | **Fellowship / micro-course audit (CEO):** [FELLOWSHIP_ENROLLMENT_DISCONNECT_AUDIT.md](./FELLOWSHIP_ENROLLMENT_DISCONNECT_AUDIT.md) — `courses.listAll` can show hardcoded 26 courses when DB empty; `enrollWithPayment` requires `microCourses` rows → **"Course not found"**; pricing **800 KES** in code/seed vs agreed **200 KES**; Home hub routes more traffic to `/fellowship` (surfaces bug, does not break Daraja). | `71086a4` |
| 2026-04-12 | Cursor | **Ship readiness handoff (final validation doc):** [SHIP_READINESS_CURSOR_VALIDATION.md](./SHIP_READINESS_CURSOR_VALIDATION.md) — corrected Manus SQL vs Drizzle schema (`enrollmentStatus`, `createdAt`, `promoCodes` field names, certificates columns, `courses` has no AHA price column); use **`pnpm run db:ship-readiness`** for read-only checks; conditional verdict, PALS PSoT vs `pricing.ts`, GHA/E2E doc gaps called out. | `d534869` |
| 2026-04-12 | Cursor | **Analytics truth loop (CLI ↔ Admin Reports):** Extracted **`server/lib/admin-analytics-rollup.ts`** — same bucketing as **`adminStats.getReport`** for `analyticsLastDays` and `resusGpsAnalyticsLastDays`. **`pnpm run verify:analytics`** now loads rows in the rolling window and uses that rollup (fixes drift vs old SQL `GROUP BY eventType` only). Unit tests in `admin-analytics-rollup.test.ts`. Updated [EVENT_TAXONOMY.md](./EVENT_TAXONOMY.md) + [SPRINT_1_IMPLEMENTATION_CHECKLIST.md](./SPRINT_1_IMPLEMENTATION_CHECKLIST.md). | `77810c5` |
| 2026-04-12 | Cursor | **Auditable enrollment integration tests (Manus-aligned):** Added RTL + jsdom devDependencies; Vitest `include` covers `*.tsx` / integration tests; jsdom only for `client/src/components/**` (lib tests stay `node`). Rewrote `EnrollmentModal.integration.test.tsx` (17 cases): mocks `MpesaReconciliationStatus`, fixed `trpc.useUtils` → `getUserEnrollments`, relative imports, `afterEach(cleanup)`. Script `pnpm run test:enrollment-integration`. Doc [MANUS_REPORT_AUDITABLE_DELIVERY.md](./MANUS_REPORT_AUDITABLE_DELIVERY.md). | `fad6f51` |
| 2026-04-11 | Cursor | **Render build fix:** `EnrollmentModal` imported **default** from `MpesaReconciliationStatus`, which only **named**-exports the component — Vite/Rollup failed on deploy. Switched to `import { MpesaReconciliationStatus }`. | `8c1bcb8` |
| 2026-04-11 | Cursor | **Login “Failed query” on `users`:** Aiven DB was missing **`users.passwordHash`** (schema behind app). Ran **`pnpm run db:fix-users`** to add column; added **`pnpm run db:check-users-columns`** to verify parity with `drizzle/schema.ts`. If sign-in fails elsewhere with the same error, run **`db:fix-users`** (or full migrations) against that environment’s `DATABASE_URL`. | `d8a956c` |
| 2026-04-11 | Manus | **Frontend enrollment integration tests (30+ cases):** EnrollmentModal component tests covering all 3 payment paths (M-Pesa, admin-free, promo-code), navigation, edge cases, error handling, loading states. Tests validate role-based access, discount calculation, phone validation, promo code validation. | `204aa0e` |
| 2026-04-11 | Manus | **E2E enrollment flow tests (Playwright):** Full user journeys from course selection → enrollment modal → payment → certificate. Tests: admin-free path (no payment), promo code path (discount validation), M-Pesa path (STK push + callback simulation), certificate issuance, double enrollment prevention, course navigation. | `204aa0e` |
| 2026-04-11 | Manus | **M-Pesa reconciliation UX + callback handler:** MpesaReconciliationStatus component with real-time polling (5s intervals, 2min max), manual reconciliation trigger, payment details display. Webhook callback handler: STK completion processing, payment status updates (completed/failed), certificate issuance, analytics tracking, owner notification, idempotency checking. | `204aa0e` |
| 2026-04-11 | Cursor | **Micro-course enrollment analytics + launch docs:** `enrollment.enrollWithPayment` emits **`course_enrollment`** (`source: enroll_with_payment`, micro-course fields) for admin-free / promo / M-Pesa; **`payment_initiation`** after successful STK on M-Pesa path. Mocked analytics in `enrollment-payment-flows.test.ts` to avoid DB noise. Updated [EVENT_TAXONOMY.md](./EVENT_TAXONOMY.md), [ENGINEERING_ACCEPTANCE_CHECKLIST.md](./ENGINEERING_ACCEPTANCE_CHECKLIST.md) (pre-launch section), [RENDER_PREDEPLOY_LOCKED.md](./RENDER_PREDEPLOY_LOCKED.md) (schema parity). | `56cf59e` |
| 2026-04-11 | Manus | **Enrollment payment flow tests:** 13 comprehensive Vitest tests for M-Pesa (STK push, phone validation), admin-free (role-based, takes priority), promo-code (discount, usage tracking, expiry/max-uses edge cases). All passing. Covers double-enrollment prevention, invalid courses, and flow priority logic. | `b304e96` |
| 2026-04-11 | Job | **Render:** `DATABASE_URL` updated and saved to match Aiven (same DB as local `.env`). Trigger or wait for API **redeploy** if Render only applies env on deploy. | — |
| 2026-04-11 | Cursor | **Aiven DB fresh migrate + committed snapshot:** Job fixed `DATABASE_URL`; `pnpm run db:fresh-migrate` completed (117 tables dropped, migrations applied). Committed [DB_SNAPSHOT_AUTOGEN.md](./DB_SNAPSHOT_AUTOGEN.md) (before/after schema, no credentials) for the team. **Deploy:** set Render `DATABASE_URL` to match working `.env`. | `348a10d` |
| 2026-04-11 | Cursor | **Pre-release DB reset tooling:** Deduped `renewalReminderSentAt` in `drizzle/0026_white_toad.sql` (column already added in `0026_certificate_renewal_reminder_sent_at.sql`). Added `scripts/db-snapshot-and-fresh-migrate.mjs` and `pnpm run db:fresh-migrate` — writes `docs/DB_SNAPSHOT_AUTOGEN.md`, drops all tables, runs `drizzle-kit migrate`. **Local run failed:** Aiven `ER_ACCESS_DENIED` — Job must paste current connection URI into `.env` / Render after any Aiven password reset, then re-run. | `d85f162` |
| 2026-04-04 | Cursor | **Parallel backlog initiative:** [PARALLEL_BACKLOG_FELLOWSHIP_AND_SCALE.md](./PARALLEL_BACKLOG_FELLOWSHIP_AND_SCALE.md) (criteria for simultaneous work), [PARALLEL_BACKLOG_EXECUTION_PLAN.md](./PARALLEL_BACKLOG_EXECUTION_PLAN.md) (FB-DB-1…FB-OPS-1), [MESSAGE_FOR_MANUS_PARALLEL_BACKLOG.md](./MESSAGE_FOR_MANUS_PARALLEL_BACKLOG.md); README + TEAMWORK_TASK_FORMAT links. **Shipped in same batch:** Care Signal **`careSignalEvents`** table + `db:apply-0031`, persist + **`care_signal_submission_created`** analytics. Manus: FB-AN-2, FB-OPS-1. | `a300c75` |
| 2026-04-04 | Cursor | **Care Signal (rename from Staff Safety Signal):** Provider product **Care Signal** — `/care-signal`, `/care-signal-analytics`; tRPC **`careSignalEvents`**; `CareSignal.tsx`, `CareSignalForm`, `CareSignalLogger`; redirects from `/safe-truth` + `/safe-truth-analytics`. Parent **Safe-Truth** unchanged (`/parent-safe-truth`). Docs/PSoT/EVENT_TAXONOMY `care_signal_*`. | `8962652` |
| 2026-03-31 | Cursor | **Fellowship v2 (SoT):** Automation-only Fellow; **Staff Safety Signal** (provider) vs parent **Safe-Truth** — §3 new row; **§17** rewritten (pillars, grace/catch-up/reset, cumulative UX, contact consent, no fellowship fee, future **Paeds Resus accredited facilities** list not rankings, legal §10, **§11 launch checklist**). [FELLOWSHIP_QUALIFICATION_AND_PROVIDER_INTELLIGENCE.md](./FELLOWSHIP_QUALIFICATION_AND_PROVIDER_INTELLIGENCE.md) v2.0; §15.5 no manual fellow; [EVENT_TAXONOMY.md](./EVENT_TAXONOMY.md) planned `staff_safety_*`. | `bac0fdd` |
| 2026-03-31 | Cursor | **Fellowship + profile + intelligence (SoT):** [FELLOWSHIP_QUALIFICATION_AND_PROVIDER_INTELLIGENCE.md](./FELLOWSHIP_QUALIFICATION_AND_PROVIDER_INTELLIGENCE.md) — fellow criteria (full course set + 3 ResusGPS cases/condition + 1 Safe-Truth–eligible event/month × 24 months, near misses OK); enrollment profile gate (cadre, facility, department, country, region, town); internal-only use for regional/facility insight and research; suggested adds (consent, verification, retention). **PLATFORM_SOURCE_OF_TRUTH.md** new **§17** + §3/§15.5 links; README + COURSE_PORTFOLIO changelog. | `6ef28c7` |
| 2026-03-31 | Cursor | **MICRO_COURSE_CATALOG_BACKLOG.md:** 24 named ADF micro-course slots (M1–M24), domains A–F + cross-cutting (seriously ill child · cardiac arrest bookends), full circulatory MECE list (10), airway/respiratory/neuro/metabolic/trauma balance; linked from COURSE_PORTFOLIO, PLATFORM_SOURCE_OF_TRUTH, README. | `128c5a1` |
| 2026-03-31 | Cursor | **Certificate feedback + portfolio strategy:** Pre-download **feedback** (rating + improvements) before PDF download — `certificateDownloadFeedback` table, `certificates` router + `LearnerDashboard` + `CertificateDownloadFeedbackDialog`. **PDF logo:** `prepareLogoForCertificatePng` knocks out near-white / near-black mattes so brand mark blends with cream panel; prefers `paeds-resus-certificate-logo.png`. **Docs:** `COURSE_PORTFOLIO_AND_ADF_STRATEGY.md` (MECE BLS/ACLS/PALS/ADF micro-courses, domains, incentives); **PLATFORM_SOURCE_OF_TRUTH** link + last-structural-update; **README** doc map; **RENDER_PREDEPLOY_LOCKED** §0030. **Migration:** `pnpm run db:apply-0030` (`drizzle/0030_certificate_download_feedback.sql`) — `0029` remains enrollments `courseId`. | `0f40e92` |
| 2026-04-03 | Cursor | **Certificate PDF logo:** Dedicated mark `client/public/paeds-resus-certificate-logo.png` (shield / cross / EKG / figure asset) resolved **before** header brand files; `certificate-pdf` `resolveLogoPngBytes` uses ordered filename list across `server/public`, `dist/public`, `client/public`. **PLATFORM_SOURCE_OF_TRUTH** §16.1 updated. | `5e79616` |
| 2026-04-03 | Cursor | **Certificates + strategy doc:** PDF header tagline **neutral** (`Clinical training · Paediatric emergency care`, not fellowship/Safe-Truth). **Body text** uses `courseDisplayName` when set (fixes PALS micro-courses still saying “Pediatric Advanced Life Support…”). **BLS** template hours **6**. **Logo:** brand mark aligned with user asset; **`prepareLogoForCertificatePng`** skips knock-out when PNG has alpha. **PLATFORM_SOURCE_OF_TRUTH** §3 courses/fellowship rows, new **§15.5** (clinical journey / ADF alignment, digital-first, no application-fee baggage), §16.1 updated. Test: PALS + `courseDisplayName`. | `758760f` |
| 2026-04-03 | Cursor | **Certificate PDF logo path (production):** Resolve brand mark from **`dist/public`** first (`path.relative` to bundled `dist/index.js`) so deployed servers find `paeds-resus-logo-brand.png` after Vite build; `client/public` paths remain fallbacks. | `b0f155c` |
| 2026-04-03 | Cursor | **Certificate PDF ESM + PSS I paginated learning:** Fixed `__dirname is not defined` in production (`certificate-pdf.ts` uses `import.meta.url`); **Paediatric Septic Shock I** — `pagedSepticShockModule` on `LearningPath`, `PssModulePagedReader` + `buildPssModuleSteps` with interleaved **formative** MCQs (recognition, fluids, escalation); graded quiz gated until flow complete. | `2fa6974` |
| 2026-04-03 | Cursor | **Learner dashboard + role switch + course UX:** Certificate download — toast on failure/silent `success:false`, `appendChild`+click for save; list shows **catalog course title** (join enrollment→course); PDF filename + programme line use `courseDisplayName`; **`useUserRole`** sync init from `localStorage` fixes institutional→provider stuck on hospital admin; **Home** backup storage check; **Header** `location.assign("/home")` when switching institution→provider; **ScrollToTop** respects `#hash` (e.g. `#my-certificates`); **LearningPath** scrolls panel into view on module/quiz; **ModuleHtmlSections** splits module HTML on `<h2>` into cards. | `d2bc9d8` |
| 2026-04-03 | Cursor | **Certificate brand mark + QR:** `client/public/paeds-resus-logo-brand.png` (PR simplified logo); `sharp`-based knock-out of near-black background for cream PDF; bottom-right **QR** to `www.paedsresus.com/verify?code=…`; `verifyCertificateByVerificationCode` + `certificates.verifyByCode`; public **`/verify`** page; **PLATFORM_SOURCE_OF_TRUTH** §16.1 updated. | `b341004` |
| 2026-04-03 | Cursor | **Certificates + learning UX (handoff):** `saveCertificate` uses branded `certificate-pdf` (same as download); PNG logo resolution validates magic bytes; `LearnerDashboard` download spinner keyed by certificate **row id** (§16.6); `LearningPath` module pick resets quiz state + “Continue: {next title}”; `CoursePaediatricSepticShock` ResusGPS orientation card; `certificates.test` targets branded PDF + DB-optional stats; **PLATFORM_SOURCE_OF_TRUTH** §16.1 issuance/download + logo filenames. **Also:** PSS I catalog title + module HTML (policy-first, fluids/FEAST/shock plain language, ResusGPS); `recordQuizAttempt` uses DB `passingScore` and returns `passed`/`passingScore`. | `9023b99` |
| 2026-04-03 | Cursor | **Course learning UI polish:** `LearningPath` — brand teal/orange, calmer stats cards, `max-w-4xl` reading width, hero + progress bar, module list with step badges, prose styling for HTML content, quiz option cards with selected state, `variant="cta"` CTAs; `CoursePaediatricSepticShock` + `CourseSeriouslyIllChild` aligned shells (gradient intro, loading spinner, brand surfaces). | `f96de65` |
| 2026-04-02 | Cursor | **M-Pesa STK false “payment not completed” before PIN:** Reconcile only marks `failed` on explicit Daraja cancel (`1`, not transport); treat 2031/1037/unknown as non-terminal; `queryStk` catch uses `QUERY_TRANSPORT_ERROR` not `1`. Client delays status poll 2.5s after STK id. Tests: `mpesa-reconciliation.test.ts`. | `9e87c5d` |
| 2026-04-02 | Cursor | **Production enrollments failing (missing `courseId` column):** `docs/RENDER_PREDEPLOY_LOCKED.md` §0029; clearer `enrollment.create` error when DB lacks `enrollments.courseId`; E2E septic doc notes production must run `pnpm run db:apply-0029`. | `f17ebd9` |
| 2026-04-02 | Cursor | **Clinical protocols docs + ResusGPS alignment:** `docs/clinical-protocols/` (README, RESUSGPS_REGISTRY, `_TEMPLATE`, `paediatric-septic-shock.md`); `docs/E2E_PAEDIATRIC_SEPTIC_SHOCK_COURSE.md`; README documentation map; code pointers in `pathways/shock.ts` and `abcdeEngine.ts`. | `4c1e1e4` |
| 2026-04-02 | Cursor | **Paediatric septic shock micro-course (Option A):** `enrollments.courseId` + migration `pnpm run db:apply-0029`; PALS `pricingSku` `pals` vs `pals_septic` on `enrollment.create`; `ensurePaediatricSepticShockCatalog` + quizzes; certificate/completion scoped to enrolment course; `/course/paediatric-septic-shock`, `LearningPath` `courseId`, Payment redirect, **200 KES** `pals_septic` in `pricing.ts`; seriously-ill page resolves enrollment by course (not “latest PALS”); learner dashboard CTA; `getPersonalizedPath` (learning + learningPath) filters PALS to enrolment course + per-module progress. | `2bb0a6b` |
| 2026-04-01 | Cursor | **PLATFORM_SOURCE_OF_TRUTH §15:** Business strategy and market context (Kenya/LMIC job-search certification dynamics, ResusGPS + condition-focused micro-courses as primary individual scale path, institutional systems/consultancy vs seat-selling only, long-range readiness signal as aspirational/governed). §3 Courses row updated; §14 extended for leadership updates to §15. | `6a6581f` |
| 2026-04-01 | Cursor | **Safe-Truth age + home hub:** Parent and provider Safe-Truth forms support **neonate / infant (months) / child (years)**; `childAge` sent as approximate **months** with full detail in `presentation` (`client/src/lib/safetruth-age.ts`). **Provider `/safe-truth`** copy clarifies provider-only vs parent story. **`/home`:** first card opens **ResusGPS** (`/resus`) instead of `/` (was redirect loop); second card **Safe-Truth (providers)** → `/safe-truth`. **`safeTruthEvents.logEvent`:** allow `parent-observation` when `userType === parent`. | `ff52d41` |
| 2026-04-01 | Cursor | **Parent Safe-Truth UX + global ResusGPS:** `/parent-safe-truth` visual refresh (brand surface, hero gradient, cards, `cta` buttons). Copy and form labels rewritten for Kenyan parents/caregivers (plain language, Kiswahili/English optional note); `ParentSafeTruthForm` uses shared `Input`/`Select`, parent-friendly option labels with preserved API values. **Header:** `ResusGPS` → `/resus` first in role nav (provider/parent/institution), anonymous Explore + mobile Explore, account menu link. | `ebd14a3` |
| 2026-04-01 | Cursor | **Instructor portal track:** Resources list (`instructorResources.ts`); assignments show **Upcoming/Past**, hospital **contact** (name, mailto, tel); `getMyAssignments` returns institution contacts + client sort. Email template `instructorSessionAssigned` + `notifyInstructorSessionAssigned` on create schedule (if instructor) and update when instructor assignment **changes**. | `3867901` |
| 2026-04-01 | Cursor | **Learner dashboard instructor CTAs:** `Enroll` anchors `#course-instructor`; provider hub “Courses & certification” links **Continue: Instructor Course** (when enrolled), **Enroll: Instructor Course**, **Instructor portal**; shows instructor number + pending approval when certified. | `1f188fb` |
| 2026-04-01 | Cursor | **Instructor journey + portal (MVP):** Extended `programType` with `instructor`; `users.instructorNumber`, `users.instructorCertifiedAt`; migration `pnpm run db:apply-0028`. `ensureInstructorCourseCatalog` + Enroll/Payment/`/course/instructor` LearningPath. Certificate issues after module completion + payment; assigns `INS-{year}-{userId}`; `completeModule` retries certificate issue. `instructor` router (`getStatus`, `getMyAssignments`); `/instructor-portal` + Header link. B2B: `listAssignableInstructors` / `assertApprovedInstructorUser` require certified + approved; Admin Approve gated on instructor number. PLATFORM_SOURCE_OF_TRUTH §5/§9 + surfaces. TS fixes: Header `userType`, Home redirect, ResusGPS interventions, `db` pool cast, Admin M-Pesa reconcile error display, admin-stats user query, certificate PDF template for instructor. | `eb7e98b` |
| 2026-04-01 | Cursor | **Security baseline + B2B instructor assignment (before v4):** Admin audit log uses **parsed procedure `input`** for key summary (`summarizeAdminProcedureInput` in `server/_core/trpc.ts`); SECURITY_BASELINE.md updated. **B2B:** `users.instructorApprovedAt` + `pnpm run db:apply-0027` / `drizzle/0027_*.sql`; `adminStats.setInstructorApproval`; Admin → Reports user list Approve/Revoke; `institution.listAssignableInstructors`; `createTrainingSchedule` / `updateTrainingSchedule` accept `instructorUserId` (approved users only); `getTrainingSchedules` joins instructor name. Hospital Admin schedule form + session table updated. | `9f74956` |
| 2026-04-01 | Cursor | **CEO priority order (1→2→3 slice):** (1) **Analytics baseline:** `adminStats.getReport` now uses **rolling N×24h from now** for `analyticsEvents` (`server/lib/report-time-windows.ts`), matching PLATFORM §8; `pnpm run verify:analytics` for DB spot-check; Admin Reports copy clarified; EVENT_TAXONOMY + Sprint 1 checklist updated. (2) **Staging discipline:** [STAGING_DEPLOYMENT.md](./STAGING_DEPLOYMENT.md) § Weekly operator checklist. (3) **Institutional payment clarity:** Hospital Admin bulk-enrollment card explains schedule vs pay path. | `c8059e3` |
| 2026-04-01 | Cursor | **Hospital admin — attendance ↔ roster + UX:** `upsertTrainingAttendance` syncs institutional staff `enrollmentStatus` / `certificationStatus` / dates from session actions (`registered` → enrolled when was pending; `attended` → completed + cert in progress when was not started). `registerAllStaffForTrainingSession` bumps remaining `pending` roster to `enrolled`. **Client:** Roster refetch + invalidate staff/stats; per-row loading instead of disabling whole roster; “Register all” shows spinner; **Add staff member** form ( `addStaffMember` ). **Schedule:** Clearer labels for datetime vs optional HH:MM; note on multi-day gap. **Copy:** Progress tab explains attendance vs individual status. | `228deac` |
| 2026-04-01 | Cursor | **Hospital Admin Dashboard:** Auto-seed minimal `courses` rows for BLS/ACLS/fellowship (and PALS via existing `ensurePalsSeriouslyIllCatalog`) before `createTrainingSchedule` / program change on `updateTrainingSchedule`, fixing “No course catalog entry…” when creating sessions. **Progress tab:** Replaced mock 45/100 and fake names with aggregates from `getTrainingSchedules` (registered vs capacity per program) and individual rows from `getStaffMembers` (enrollment + certification badges, approximate progress). **UI:** Brand gradient/surfaces, semantic KPI colors, ROI placeholder until analytics; schedule copy + accessible hint for optional instructor name (display-only on session/roster). | `6151f56` |
| 2026-04-01 | Cursor | **Institutional onboarding UX + React #185:** Program step used parent `onClick` + Checkbox `onCheckedChange` (double toggle → max update depth). Replaced with `<label>` + single `onCheckedChange`. **Select** uses `undefined` when type unset (Radix). Theme: brand surfaces, `cta` buttons, calmer copy; country editable with Kenya default; hero no longer BLS/ACLS-only. | `96acf5b` |
| 2026-04-01 | Cursor | **Institutional onboarding URL typo:** Dashboard buttons used `/institution-onboarding` while the app route is `/institutional-onboarding`, so the catch-all showed ResusGPS. Fixed navigates in `HospitalAdminDashboard` + `InstitutionalPortal`; added `Redirect` from `/institution-onboarding` → `/institutional-onboarding`. | `2564f8d` |
| 2026-04-01 | Cursor | **PALS `/course/seriously-ill-child` empty:** Server `ensurePalsSeriouslyIllCatalog` (shared with seed script); `learning.getCourses` runs it when `programType=pals` returns no rows so production self-heals. **Header:** `effectiveRole = role ?? map(userType)` so nav and role switcher work before localStorage sync and when switching institutional↔provider. **Branding:** `/favicon.png` (tab + apple-touch), theme-color `#1b3d3d`; header uses icon + text (not full wordmark). **Enroll:** removed temporary PALS “third tile / #course-pals” guide. **LearningPath:** empty catalog message. | `60a7106` |
| 2026-04-01 | Cursor | **M-Pesa STK stuck after PIN:** `getPaymentByCheckoutRequestId` / `getPaymentStatusForEnrollment` call `reconcilePaymentRowByStkQuery` for all pending M-Pesa rows (not only `MOCK_`), so DB updates when webhook is delayed or missed. **`queryStk`:** normalize `ResultCode` with `String()` so numeric `0` from Daraja counts as success. **Client:** `MpesaPaymentForm` — “I’ve paid — check status now”, `cta` button, brand-tinted status, 5s poll. **Theme sweep (shell/marketing):** Start, Institutional, PerformanceDashboard, JoinSession, CPRMonitoring, Help, About, NotFound, Footer, VideoTestimonial, RecommendationsPanel, OfflineIndicator, LearnerDashboard touches; clinical protocol pages largely unchanged. | `ada7201` |
| 2026-04-01 | Cursor | **Brand theme & readability:** CSS tokens `#1B3D3D` / `#F37021`, light default + `ThemeToggle`; `index.css` contrast (muted-foreground, surfaces); Header/Enroll/Payment/LearnerDashboard use semantic colors; `Button` variant `cta`; logo `paeds-resus-logo-brand.png`. | `dcb7679` |
| 2026-04-01 | Cursor | **Enroll UX:** Copy + anchor `#course-pals` on `/enroll` so “The systematic approach to a seriously ill child” (KES 100) is discoverable; E2E doc “where it appears”. | 3add2ec |
| 2026-04-01 | Cursor | **PALS E2E course (seriously ill child, KES 100):** `pricing.ts` PALS title + test price; `seed-pals-seriously-ill-course.ts` + `pnpm run seed:pals-course`; `CourseSeriouslyIllChild` + `/course/seriously-ill-child`; `LearningPath` + `learning.getModuleContent`/`getCourseStats`/`getCourses` fixes; `Payment` `onPaymentComplete` redirect; `docs/E2E_SERIOUSLY_ILL_CHILD_COURSE.md`; README link. | 880f72a |
| 2026-04-01 | Cursor | **Sprint 1 (Measurement Truth MVP) — implementation:** Server `trackEvent` / `trackPayment*` for **revenue + mission + B2B**: `course_enrollment` on `enrollment.create` and inline M-Pesa enroll; `payment_initiation` + `payment_completion` (webhook + `reconcilePaymentRowByStkQuery`); `safetruth_submission` on `parentSafeTruth.submitTimeline`; `institution_training_schedule_created` on `createTrainingSchedule`. **Docs:** `SPRINT_1_MEASUREMENT_TRUTH_AUDIT_RESULTS.md`; `EVENT_TAXONOMY.md` frozen; checklist + `enrollment.test.ts` `trackAnalyticsEvent` mock. **Remaining:** manual § Verification in `SPRINT_1_IMPLEMENTATION_CHECKLIST.md` (Admin Reports / DB). | 1dd79c8 |
| 2026-04-01 | Cursor | **Sprint 1 (Measurement Truth MVP) — docs:** `PRODUCT_BACKLOG_PRIORITIZED.md` Sprint 1 section + CEO sprint sequence; new `EVENT_TAXONOMY.md`, `SPRINT_1_IMPLEMENTATION_CHECKLIST.md`, `SPRINT_1_EVENT_AUDIT_TEMPLATE.md`; README links; WORK_STATUS In progress for Cursor. | c62dbd2 |
| 2026-04-01 | Cursor | **README:** “Documentation map (start here)” — ordered links to PLATFORM_SOURCE_OF_TRUTH, STRATEGIC_FOUNDATION, WORK_STATUS, AI_TEAM_WORKFLOW, ENGINEERING_ACCEPTANCE_CHECKLIST, plus `docs/archive/` note. | 5d8ee27 |
| 2026-04-01 | Cursor | **Strategy docs alignment:** `STRATEGIC_VISION_2031.md` → **`docs/archive/`** (banner + [archive/README.md](./archive/README.md)); **near-term SoT** remains [STRATEGIC_FOUNDATION.md](./STRATEGIC_FOUNDATION.md) + [PLATFORM_SOURCE_OF_TRUTH.md](./PLATFORM_SOURCE_OF_TRUTH.md). *If another machine does not see `docs/STRATEGIC_FOUNDATION.md` or commits `a0d5246`/`359d1a5`, run `git pull` on `main`.* | bd1ebd3 |
| 2026-03-31 | Cursor | **`docs/STRATEGIC_FOUNDATION.md`**: canonical **strategic** onboarding (single north-star problem, theory of change, LMIC realism, origin narrative, ResusGPS↔learning, institutions/Safe-Truth/Book of the Unforgotten, triage-before-admin pattern, honest outcome claims). **`docs/PLATFORM_SOURCE_OF_TRUTH.md`** links to it from purpose + §2. | a0d5246 |
| 2026-03-31 | Cursor | **`docs/PLATFORM_SOURCE_OF_TRUTH.md`** expanded: single onboarding source for **who we are / why / what / for whom**; mission; offerings table; audiences; surfaces map; stack; guardrails; all prior technical decisions preserved (auth, reports EAT, funnel, deployment, priorities). | 3fcc96e |
| 2026-03-31 | Cursor | M-Pesa STK **AccountReference** (phone “Account no.” line): was **`userId` only** (e.g. `1` → “Account no. 1”). Now **`buildStkAccountReference`**: learner **first name** + **`E{enrollmentId}`** (e.g. `MARYE42`), max 12 chars, normalized in `mpesa-real`. Retry uses same helper. | 6819998 |
| 2026-03-31 | Cursor | M-Pesa STK: **`getDarajaTimestampNairobi()`** (`server/lib/daraja-timestamp.ts`) — Daraja password timestamp must be `YYYYMMDDHHmmss` in **Africa/Nairobi (EAT)**; UTC (`toISOString`) or server TZ (e.g. Render UTC) caused **400.002.02 Invalid Timestamp**. Wired in `mpesa-real.ts` (STK + query) and `services/mpesa.ts` `getTimestamp()`. | 5bc8c77 |
| 2026-03-25 | Cursor | M-Pesa: `resolveStkCallbackUrlFromEnv` appends `/api/payment/callback` when `MPESA_CALLBACK_URL` is origin-only (fixes Daraja HTTP 400); clearer 400 errors + Daraja body in logs; `.env.example` notes `MPESA_ENVIRONMENT=production` for prod keys. | 7b70506 |
| 2026-03-25 | Cursor | M-Pesa: canonical Daraja STK callback path **`/api/payment/callback`** (Safaricom naming); `server/lib/mpesa-callback-path.ts`; Express mounts payment + legacy mpesa alias; defaults in `mpesa-real`, `services/mpesa`, `mpesa` router use `defaultStkCallbackUrl`. Docs updated. | 13aa3a7 |
| 2026-03-25 | Cursor | M-Pesa 404 diagnosis: `VITE_TRPC_URL` optional in `main.tsx` when www/API split; `MPESA_CALLBACK_URL` default from `APP_BASE_URL` + `/api/mpesa/callback` (not example.com); clearer Daraja 404 message + server log `url=`. Docs callback path vs Manus `/api/payment/callback` typo. | b4ec87f |
| 2026-03-25 | Cursor | M-Pesa: `server/mpesa.ts` now uses **real Daraja** (`mpesa-real`) by default so `mpesa.initiatePayment` triggers STK on phone; mock only when `MPESA_USE_MOCK` or Vitest. `mpesa-real` accepts `DARAJA_*` keys and `MPESA_PAYBILL` as shortcode fallback. Docs `.env.example` + `MPESA_CONFIG_REFERENCE.md`. | b03694e |
| 2026-03-25 | Cursor | Ops: `pnpm run db:apply-0026` idempotent script + `RENDER_PREDEPLOY_LOCKED.md` section for production missing `certificates.renewalReminderSentAt` (fixes Render/Aiven ER_BAD_FIELD_ERROR). | 1f1792a |
| 2026-03-25 | Cursor | HI-B2B-1: `updateTrainingSchedule`, `deleteTrainingSchedule` (tenant-scoped; delete clears `trainingAttendance` for that session); `getTrainingSchedules` joins `courses` for `programType`. Hospital Admin **Schedule** tab: edit dialog, mark cancelled, delete confirm. HI-PLAT-2 slice: `server/lib/structured-log.test.ts`. | 77ab344 |
| 2026-03-25 | Cursor | HI-PLAT-1 / HI-SAFE-1 / HI-CLIN-3 / HI-ENT-1 (slices): `server/lib/structured-log.ts` + `logStructured` on M-Pesa STK callback and Safe-Truth `markResponseReady`; `getSafeTruthStats.reviewedSubmissionsCount` + prominent alert on `ParentSafeTruth`; `adminStats.getReport.resusGpsAnalyticsLastDays` (`resus_*` events) + Reports UI card; `adminStats.getAdminAuditLog` + CSV + preview on Admin Reports; Safe-Truth response-ready email line about spam folder. | 50d44ed |
| 2026-03-25 | Cursor | HI-B2B-2: training attendance — `getTrainingAttendanceForSchedule`, `upsertTrainingAttendance`, `registerAllStaffForTrainingSession` in `institution` router; Hospital Admin Schedule tab **Roster** UI; `enrolledCount` synced from attendance rows. Vite `chunkSizeWarningLimit` 900 for deploy chunk warning. | d2c28f8 |
| 2026-03-25 | Cursor | Render deploy: fixed esbuild error in `server/routers/enrollment.ts` — mixed `??` and `||` without parentheses in `adminConfirmOfflinePayment`; clarified as `(input.amountPaid ?? pendingSum)` then fallback to `enrollment.amountPaid` and `0`. | 01a48f0 |
| 2025-03-07 | Manus | Phase 1: Analytics Instrumentation — created useResusAnalytics hook, integrated event tracking at ResusGPS lifecycle points (assessment start, questions, interventions, cardiac arrest, ROSC), events flow to analyticsEvents table, admin reports now show real activity | 7041295, 337aa52 |
| 2025-03 | Manus | Phase 2: Staging Environment — STAGING_DEPLOYMENT.md with branch model (main/develop), manual deploy workflow, staging Render/Aiven setup guide, PR verification checklist, rollback procedure | (pushed) |
| 2025-03 | Cursor | Phase 3: Security baseline — SECURITY_BASELINE.md; password complexity (min 8 chars, one letter + one number); session max age via SESSION_MAX_AGE_MS (default 1 year); adminAuditLog table + middleware; EAT for admin reports "this month" (startOfMonthEAT/endOfMonthEAT). | $h |
| 2026-04-11 | Cursor | **Care Signal streak + Fellowship tests:** Added `server/routers/fellowship-care-signal-streak.ts` with `computeCareSignalStreak` (forward EAT month order, rolling 24 months; grace rows for all calendar years in window). `fellowship.ts` uses it; grace query now uses `inArray(grace.year, windowYears)`. **ResusGPS:** cases query uses `inArray(cases.sessionId, sessionIds)` (correct OR across sessions; multiple `eq(sessionId, …)` in `and()` matched no rows). Tests import shared streak helper; **14/14** `fellowship.care-signal-pillar.test.ts` pass. (On Manus `419890c`, the same test file reported **8/14 failing** before this follow-up.) | `3eba9ab` |

---

## In progress

| Who | What |
|-----|------|
| Cursor | Best-way-forward execution tranche complete in code/docs and pushed; awaiting production redeploy smoke confirmation on the latest commit set. |
| Cursor | Governance closeout support: Phase 0/Week 1-4 execution complete with evidence pack prepared; awaiting Job-led P0-7 sync sign-off window for formal Go/No-Go recording. |
| Manus | Phase 4: ResusGPS v4 clinical upgrades (undo, medication dedup, multi-diagnosis, structured age, countdown timers, dose rationale). **Note:** CEO priority order puts analytics baseline before v4; align v4 timing with [`PLATFORM_SOURCE_OF_TRUTH.md`](./PLATFORM_SOURCE_OF_TRUTH.md) §12. |

**Sprint 1 note:** Implementation + taxonomy freeze merged; optional **Verification** (Admin Reports / `analyticsEvents`) remains in [`SPRINT_1_IMPLEMENTATION_CHECKLIST.md`](./SPRINT_1_IMPLEMENTATION_CHECKLIST.md).

---

## Blocked / needs decision

| Item | Blocking reason or decision needed |
|------|-------------------------------------|
| (add when stuck or when CEO/product decision needed) | |

---

## Critique / review

Any agent can add here. Format: date, reviewer (Codex/Manus/Cursor), subject (e.g. Manus Phase 1 analytics), what you checked, and any issues or suggestions. This lets others scrutinize and build on each other's work without Job pasting responses.

| Date | Reviewer | Subject | Notes |
|------|----------|---------|-------|
| 2026-04-16 | Cursor | Production vs local credentialed smoke reconciliation | **Checked:** credentialed smoke on production and local after BWF Phase 2/3 edits. **Finding:** production initially showed post-login drift to parent lane when prior browser `userRole` persisted across sessions; local fix now resets stored role on login/register so post-login follows default `userType` and full provider/parent/institution/instructor smoke passes. |
| 2026-04-16 | Cursor | Manus institutional-gap audits | **Checked:** both Manus reports against the current repo and live product. **Conclusion:** directionally useful for institutional-grade ambition, but not reliable as raw blocker lists because they repeat multiple false or overstated claims (e.g. no institutional dashboard, no instructor portal, no pricing/quote surface, no About/Help, no parent confirmation). Consolidated all audit inputs into `docs/FINAL_BEST_WAY_FORWARD_PLAN.md` with a truth filter and phased execution order. |
| 2026-04-16 | Cursor | Manus UX re-audit reconciliation | **Checked:** Manus' reported blockers against the current repo and live app. **Rejected as stale/inaccurate:** missing React imports in `Login.tsx`, visible login-error failure, active OAuth redirect on `/start`, and large TypeScript regression. **Confirmed and executed:** provider home was still using fragmented data sources and needed a consolidated summary path; protected-route and ResusGPS UX still needed stronger copy/states. Follow-up implementation completed and verified. |
| 2026-04-13 | Cursor | Post-TypeScript-fix UX/UI re-audit | **Checked:** live anonymous/protected-entry flows and UX-critical code paths after the TypeScript cleanup. **Confirmed:** login errors now render user-friendly inline copy; role-aware route protection is working. **Issues:** provider home still lacks a consolidated summary router and can show the wrong primary CTA while three queries are loading; protected-route redirects remain 3-4s with weak loading feedback; enrollment query failures are collapsed into empty arrays, masking dashboard errors. See `docs/UX_UI_REAUDIT_POST_TYPESCRIPT_FIXES.md`. |
| 2026-04-14 | Cursor | Manus + Job Phase 0 review (`fec8b5a`, `ac2aeb9`) | **Accepted core critique:** execution plan needed active owner activation, profiling methodology, access matrix specificity, dependency DAG granularity, test automation scope, rollback operational detail, and scheduled sign-off. Implemented concrete Phase 0 artifacts and timeline lock; moved tasks to in-progress where execution has started. |
| 2026-04-01 | Cursor | **M-Pesa mock / test rows in DB** | Do **not** bulk-delete real `payments` / `enrollments` on production (audit, finance, certificates). Rows from dev **`MOCK_` CheckoutRequestID** or obvious test users may be removed in **non-prod** only after backup. Prefer **admin reconciliation** + marking failed duplicates over deleting live payment history. |
| 2026-04-01 | Cursor | Manus “repo alignment” audit vs `main` | On latest `main`, `docs/STRATEGIC_FOUNDATION.md` exists; `PLATFORM_SOURCE_OF_TRUTH.md` links it (not 56-line stub); commits `a0d5246` / `359d1a5` are in history. Likely **stale clone or wrong branch** if missing. **Root** `STRATEGIC_VISION_2031.md` was valid concern—**archived** under `docs/archive/` with explicit “not execution truth” framing. |
| 2026-04-11 | Cursor | Manus Phase 3 (`419890c` area) | **Verified:** join uses string `sessionId`; Care Signal pillar + PSoT edits + test file exist on `main`. **Issue:** `pnpm test -- fellowship.care-signal-pillar.test.ts` was **8/14 red** on merged code: duplicated streak helper in tests did not match router; year-to-January walk + implicit grace inflated counts. **ResusGPS:** `and(...sessions.map(eq sessionId))` is incorrect for multiple sessions (needs `inArray` / `or`). Cursor follow-up extracts shared streak logic and fixes cases query; tests green. |
| 2026-04-11 | Cursor | **Manus enrollment payment flow tests (GitHub 13/13 red)** | **Local:** 13/13 pass when `DATABASE_URL` unset. **CI with `DATABASE_URL`:** `ensureMicroCoursesCatalog` called `db.query.microCourses.findFirst`, but `drizzle(pool)` is created without `{ schema }`, so `db.query.microCourses` was **undefined** — failure before mocked `getCourseDetails` / promo logic (symptoms: wrong error text, `paymentMethod` undefined, `incrementPromoCodeUsage` not called). **Fix:** catalog ensure uses SQL builder only; `createEnrollment` normalizes mysql insert id to `{ insertId }`; tests partially mock `ensureMicroCoursesCatalog`. |
| 2026-04-12 | Cursor | Manus/Job ship handoff memo vs repo | **Checked:** No `.github/workflows` on `main` — “E2E on every push” is not in-repo unless hosted elsewhere. Memo SQL used wrong column names — superseded by [SHIP_READINESS_CURSOR_VALIDATION.md](./SHIP_READINESS_CURSOR_VALIDATION.md) + `pnpm run db:ship-readiness`. **Suggest:** add workflow + link real Playwright doc path when CI lands. |
| 2026-04-06 | Manus | FB-AN-2 + FB-OPS-1 (parallel backlog) | Migration 0031 applied to production DB; careSignalEvents verified; `pnpm run verify:analytics` validates query path (0 events expected—no submissions yet). STAGING_DEPLOYMENT.md §0031; fc89a39 merge. |
| (add when you review another's work) | | | |

---

Last structural update: 2025-02 (WORK_STATUS created).
